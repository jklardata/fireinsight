{
  "builds": [
    { "src": "static/**", "use": "@vercel/static" },
    { "src": "web.py", "use": "@vercel/python", "config": { "maxLambdaSize": "50mb", "maxDuration": 60 } }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "web.py" }
  ]
}
